{
  "name": "investment-research-committee",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm --parallel --filter @investment-research/web --filter @investment-research/api dev",
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
